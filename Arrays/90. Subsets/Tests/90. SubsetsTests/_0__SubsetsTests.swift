import XCTest
@testable import _0__Subsets

final class _0__SubsetsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(_0__Subsets().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
