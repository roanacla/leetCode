import XCTest
@testable import _80__Valid_Palindrome_II

final class _80__Valid_Palindrome_IITests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(_80__Valid_Palindrome_II().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
