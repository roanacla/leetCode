import XCTest
@testable import ___Reverse_Integer

final class ___Reverse_IntegerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(___Reverse_Integer().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
