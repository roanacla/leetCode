import XCTest
@testable import _229__Meeting_Scheduler

final class _229__Meeting_SchedulerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(_229__Meeting_Scheduler().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
