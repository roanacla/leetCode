import XCTest
@testable import _0__Valid_Parentheses

final class _0__Valid_ParenthesesTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(_0__Valid_Parentheses().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
